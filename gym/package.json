{
    "name": "@slopus/rig-gym-tests",
    "private": true,
    "type": "module",
    "scripts": {
        "check": "tsc --noEmit",
        "test": "vitest run --no-file-parallelism --testTimeout=120000"
    },
    "devDependencies": {
        "@earendil-works/pi-tui": "^0.80.3",
        "@types/node": "^24.0.10",
        "typescript": "^5.8.3",
        "vitest": "^3.2.4"
    }
}
